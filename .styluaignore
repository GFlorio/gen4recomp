# Derived output and scratch space are not source, and must not fail the
# formatting check. Kept separate from .luarc.json workspace.ignoreDir: the
# analyzer also skips directories that hold no Lua at all.
.cache/
.agents/
data/generated/
data/scripts/overrides/
import-output/
tmp/
vendor/lua-cats-love2d/
